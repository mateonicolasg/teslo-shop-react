# TesloShop

1. Clonar el repositorio
2. Clonear el archivo .env.template y renombrarlo a .env
3. Instalar dependencias con `npm install` para instalar las dependencias
4. Verificar que el backend este corriendo en el puerto 3000
5. Ejecutar el proyecto con `npm run dev` para iniciar el servidor de desarrollo